#!/usr/bin/make -f
%:
	dh $@

override_dh_installinit:
	dh_installinit --name=dbus-user --no-start

override_dh_systemd_enable:
	dh_systemd_enable --name=af-services
	dh_systemd_enable --name dbus-user
